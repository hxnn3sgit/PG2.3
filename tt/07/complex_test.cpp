#include <iostream>

class complex_number {
private:
	double real;
	double imag;
public:
	//konstruktoren
	complex_number(double r, double i) : real(r), imag(i) {}
	complex_number(double x) : real(x) {}
	//operatoren
	complex_number
};


int main() {

	return 0;
}
